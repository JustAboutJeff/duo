<% if flash.any? %>
  <% flash.each do |key,msg| %>
    <div data-alert class="alert-box">
      <%= content_tag(:span, msg) %>
      <a href="#" class="close">&times;</a>
    </div>
  <% end %>
<% end %>
